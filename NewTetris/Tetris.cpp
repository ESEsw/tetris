#include <stdio.h>
#include <stdlib.h>
#include <termios.h>

struct termios initial_settings, new_settings;

int main(int argc,char *argv[])
{
	srand(tume(NULL));

	new_setting = initial_setting;
	

	ststem(clear);


}
